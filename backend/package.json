{
  "name": "affectcare-backend",
  "version": "1.0.0",
  "description": "Express API bridging the AffectCare UI to the Python ML prediction pipeline",
  "main": "server.js",
  "type": "module",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "postinstall": "cd ../ML-Backend && python3 -m venv venv && ./venv/bin/pip install -r src/requirements.txt"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.21.2",
    "multer": "^2.2.0"
  }
}
